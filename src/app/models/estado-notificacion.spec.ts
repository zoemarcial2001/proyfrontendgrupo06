import { EstadoNotificacion } from './estado-notificacion';

describe('EstadoNotificacion', () => {
  it('should create an instance', () => {
    expect(new EstadoNotificacion()).toBeTruthy();
  });
});
